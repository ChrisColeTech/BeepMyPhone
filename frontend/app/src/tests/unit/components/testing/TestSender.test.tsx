import { render } from '@testing-library/react'
import { TestSender } from '../../../../components/testing/TestSender'

describe('TestSender', () => {
  it('renders correctly', () => {
    render(<TestSender />)
    // TODO: Add test assertions
  })
})

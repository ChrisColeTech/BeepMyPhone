import { render } from '@testing-library/react'
import { BulkExporter } from '../../../../components/export/BulkExporter'

describe('BulkExporter', () => {
  it('renders correctly', () => {
    render(<BulkExporter />)
    // TODO: Add test assertions
  })
})

import { render } from '@testing-library/react'
import { FilterTemplates } from '../../../../components/filters/FilterTemplates'

describe('FilterTemplates', () => {
  it('renders correctly', () => {
    render(<FilterTemplates />)
    // TODO: Add test assertions
  })
})
